// Root build script.
//
// Per-module configuration lives in the `job-search.*` convention plugins under build-logic/.
// Each module applies the convention plugins it needs; the root stays intentionally thin.
